<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>

    <div class="w-full min-h-screen bg-zinc-800 p-10">
      <a
        href="/logout"
        class="border-2 text-white bg-red-700 outline-none border-zinc-700 mb-5 inline-block"
        >LOGOUT</a
      >

      <div class="flex items-start gap-5">

        <div class="w-10 h-10 ">

          <img class="w-full h-full object-cover overflow-hidden" src="/upload/<%= preuser.profilepic %>" alt="Profile Picture">


        </div>

 
      <h3 class="text-3xl mb-5 text-white">

        <span class="font-light tracking-tighter">HELLO</span>,<%= preuser.name %>

      </h3>

      </div>


      <h5 class="text-white mb-5">You can create a post</h5>

      <form action="/post" method="post">
        <textarea
          placeholder="Write your post here"
          class="w-2/3 h-2/3 resize-none bg-transparent border-3 bg-zinc-700 outline-none min-h-[100px] max-h-[400px]"
          name="content"
        ></textarea>

        <input
          type="submit"
          class="p-2 w-20 border-3 text-white bg-blue-700 block"
          value="POST"
        />
      </form>

      <!-- -->

      <% preuser.posts.reverse().forEach (function(post){ %>

        <div class="posts mt-12">

          <h3 class="text-zinc-300">Your Posts</h3>
  
          <div class="postcontainer mt-5">
  
  
            <div class="post w-1/3 p-4 border-2 border-zinc-700 bg-zinc-600 mb-3">

              <h4 class="text-blue-500 mt-2"><%= preuser.username %></h4>
              <p class="text-sm tracking-tighter text-white"><%= post.content %></p>
               
              <small class="mt-2 inline-block"> <%= post.likes.length %>likes</small>
              <div class="flex gap-5 mt-3">
                <a href="/like/<%= post._id %>" class="text-blue-600" ><%= post.likes.indexOf(preuser._id)===-1 ? "like":"unlike" %></a>
                <a href="/edit/<%= post._id%>" class="text-zinc-400">Edit</a>
              
              </div>
  
            </div>


        <% }) %>

      </div>


    </div>

  </body>
</html>
